# Промт для следующей сессии: CampusTrace, финальный отрезок до 19.09 12:00 (Asia/Almaty)

Проект: `/Users/sarsembaevadilbek/Documents/Codex/2026-09-16/create-an-image-of/outputs/campustrace-mvp`
Репозиторий: https://github.com/sNibaAdeka/campustrace (ветка `main`). Официальный кейс: LOCUSCASE1.
Сначала выполни `TZ=Asia/Almaty date`, назови оставшееся время и работай по порядку ниже.

## Правила (важнее любой фичи)
- Нет стоковых или чужих фото, нет ручного подбора под видом автоматики, нет выдуманных цифр скорости или достоверности.
- В README и презентацию пишем только то, что замерено в этой сессии. Нет замера: пиши «не замерено».
- Соцсети: только текст и ссылка, никогда `<img>`.
- Ключи только в `.env` и в переменных хостинга. Ключ, вставленный в чат, считать раскрытым и заменить после хакатона.
- Не обходи авторизацию и лимиты. Нужен вход или ключ: остановись и спроси.

## Состояние на 18.09 22:05
- Тесты: 76, все зелёные. Живой холодный замер на 15 вузах: 0 ошибок, 0 превышений 30 с, p50 полного профиля 22 с (при `PROFILE_BUDGET_SECONDS=24`).
- Фото: Wikidata (здания вуза с типом), Commons (depicts, геопоиск, категории, категории зданий), Openverse (только CC BY, CC BY-SA, CC0, PDM).
- ИИ: vision-проверка (Groq `qwen/qwen3.8-27b`, мозаика 2×2), разбор подписей (Groq `openai/gpt-oss-20b`, не считается независимым подтверждением), сводка отзывов.
- Показатель достоверности: высокая / средняя / низкая по числу независимых видов подтверждений. Понижается при споре проверок, неясной категории и городе, не повышается.
- Не готово: **публичный деплой**, имена и роли команды, видео до 3 мин, PDF-презентация до 8 слайдов, сабмит на aistartify.com.

## Порядок работы

### 1. Деплой (блокер №1)
Render Blueprint из `render.yaml`. Нужны env: `CAMPUS_TRACE_USER_AGENT` (с контактом), `GROQ_API_KEY`. Опционально: `OPENVERSE_CLIENT_ID/SECRET`, `REDDIT_CLIENT_ID/SECRET`.
Проверка деплоя, а не «билд прошёл»:
- `curl <URL>/api/health`, `/api/integrations`;
- поиск и профиль трёх вузов через API и в браузере (скриншот галереи);
- замерь холодный старт хостинга отдельно и запиши его в README как есть.
Если free-план засыпает: решение о Starter принимает человек. Запусти `scripts/prewarm.py --base <URL> --passes 2`.

### 2. Замер на проде
`scripts/benchmark.py --base <URL> --refresh` на 15 вузах. Вставь в README новую таблицу с датой и стендом, старую оставь как «локальный замер».

### 3. Точность на вузах, которых команда не готовила (30 % оценки)
Выбери 6 вузов вне выборки (включи один с кириллицей и один региональный). На каждом возьми 10 случайных карточек и проверь вручную по первоисточнику: вуз верен? категория верна? лицензия открытая? Посчитай `верно / 10` и запиши в README «ручная проверка, N карточек, дата». Если ниже 8/10, найди причину (выбор источника, категория, портреты, дубли) и исправь.

### 4. Слабые места, которые известны
- Общежитий и аудиторий у большинства вузов нет в Commons. Панель «пробелы» с кнопкой «добавить фото на Commons» есть. Проверь, что она видна на 375 px.
- Vision и Openverse упираются в бесплатные лимиты. Не тратить квоту на отладку. Профиль при этом честно пишет «проверено N из M». Проверь, что это видно в интерфейсе, а не только в JSON.
- Один и тот же кадр из Commons и Flickr должен схлопываться по визуальному хешу: проверь на MIT (Baker House).
- Reddit без официального API даёт мало: не выдумывай отзывы. Пустой блок должен честно говорить, почему он пуст.

### 5. Материалы сабмита (все обязательны для допуска)
- README: ссылка на сайт, GitHub, запуск, тестовый сценарий, роли команды (`<ФИО>` пока плейсхолдер, не выдумывать), источники, AI/API, готовые компоненты, ограничения.
- Видео до 3 минут (сценарий в README, раздел 15): снимает человек на задеплоенном сайте, вуз не из демо.
- Презентация до 8 слайдов в PDF: текст в `docs/PRESENTATION.md`.
- Сабмит на aistartify.com с кодом `LOCUSCASE1` делает капитан.

### 6. Финальная сверка
- Клик на любое фото ведёт на первоисточник (10 случайных карточек через Playwright).
- Каждое встроенное фото имеет открытую лицензию из whitelist.
- Нет `<img>` с доменов соцсетей.
- Нет процентов вероятности без объяснения.
- `git status` чистый, всё в `main`, `.env` не в репозитории, история без ключей: `git log -p | grep -E "gsk_|xai-|pk\.eyJ|AIza"`.
- Незапланированные изменения после 11:00 не делать: заморозка кода.

## Итоговый отчёт
Что сделано (коммиты), ссылка на GitHub и прод, реальные цифры, что не успел, что осталось человеку, сколько осталось до дедлайна.
